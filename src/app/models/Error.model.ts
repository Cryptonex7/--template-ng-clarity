export interface Error {
  buildNumber: string;
  buildTimestamp: number;
  errorTimestamp: string;
  label: string;
  responseCode: string;
  responseMessage: string;
  success: string;
  threadName: string;
}
